# 引き継ぎ — Okulab Time

別の環境(Codex / Claude Code など)で作業を引き継ぐための資料。
このファイルだけを読めば、**いま何が壊っていて、何を実装すべきか**が分かるようにしてある。

- 対象リポジトリ: https://github.com/choucrin/Okulab_time
- 作業ブランチ: `claude/recording-end-validation-c7zblx`
- このファイルの時点のコミット: `34bc0ee`(= `main` と同一)
- 関連文書: [README.md](README.md)(構築・利用)/ [RSD.md](RSD.md)(要件)/ [REVIEW.md](REVIEW.md)(レビュー記録)/ [ProgressReport.md](ProgressReport.md)(開発ログ)

---

## 0. 最初に読むべき注意

**リモートは v.02.0 のままで、実機で報告された不具合は未修正。**

前のセッションで v.02.1 として修正とレビュー(51 巡)を行ったが、**push しないまま
実行環境が破棄され、コミットが全て失われた**。リモートには何も反映されていない。

この資料は、そのとき到達した設計と、レビューで見つかった欠陥の一覧を残したもの。
**同じ発見をやり直さずに済むよう、結論だけを列挙している。**

> **作業規則:** コミットしたら**その都度 push する**。
> このリポジトリは使い捨ての実行環境で編集されることがあり、
> ローカルだけの成果は失われる。

---

## 1. プロジェクトの概要

2 台の端末で「計測開始」「計測終了」を押し、その経過時間を記録する実験用の Web アプリ。

| 項目 | 内容 |
|---|---|
| 構成 | 静的 HTML / CSS / JavaScript(ビルド工程なし)+ Firebase |
| 公開 | GitHub Pages。`.github/workflows/deploy.yml` が `main` への push でのみデプロイ |
| CI | デプロイ前に `node tools/verify.js` を実行。**失敗するとデプロイされない** |
| データ | Firestore。`rooms/{roomId}/sessions/{id}` が 1 回の計測 |
| 排他 | `rooms/{roomId}/meta/current.activeSessionId` で「進行中は 1 本だけ」を保証 |
| 認証 | 匿名認証 + 合言葉。`roomId` は合言葉の SHA-256 先頭 40 桁 |
| 実機 | iPad(ホーム画面に追加した standalone 状態)。**コンソールを開けない** |

### ファイルの役割

| ファイル | 役割 |
|---|---|
| `js/app.js` | 画面制御。約 1500 行。状態・表示・操作のすべて |
| `js/store.js` | Firestore アクセス層。トランザクションと一括削除 |
| `js/clock.js` | 端末時計とサーバー時刻のオフセット推定(SNTP 方式) |
| `js/passages.js` | 被験者用画面に出す読み物 |
| `tools/verify.js` | 整合性チェック。現在 8 項目 |
| `firestore.rules` | セキュリティルール。送信フィールドを固定 |

### 画面

- **参加画面**: 合言葉と役割(開始 / 終了 / 閲覧)を入力
- **実験者用画面**: 状態表示・計測ボタン・記録一覧・CSV 書き出し・一括削除
- **被験者用画面**(終了側の端末のみ): 読み物と「気づきを検出」ボタンのみ。
  **計測の状態を一切表に出さない**(被験者は実験内容を知らされていない)

---

## 2. いま直すべき不具合(最優先)

### 症状

一括削除を実行すると、直後に `Can't find variable: COMMIT_TIMEOUT_MS`
(Chrome では `COMMIT_TIMEOUT_MS is not defined`)と表示される。実機の iPad で報告。

### 原因

`js/store.js:229` が `COMMIT_TIMEOUT_MS` を参照しているが、**宣言がどこにもない**。

```js
// js/store.js:229
await withTimeout(batch.commit(), COMMIT_TIMEOUT_MS);   // ← 未宣言
```

混入したのは `6823612`(v.01.9)。「削除の確定に打ち切りがなく、通信が切れると
無期限に固まる問題を修正」(REVIEW.md 9-03)として `withTimeout` を追加した際に、
**定数の宣言だけが漏れた**。ES モジュールは strict mode なので、実行時に
`ReferenceError` になる。

### なぜ気づきにくいか(重要)

JS は引数を左から評価するため、`batch.commit()` が**先に呼ばれて送信された後**に
`COMMIT_TIMEOUT_MS` の評価で例外になる。したがって:

- **最初の 400 件(`BATCH_SIZE`)は実際に消える**のに「失敗」として報告される
- 401 件目以降は送信されない(ループが中断する)
- 400 件を超えるルームでは、押すたびに 400 件ずつ消えながらエラーが出続ける
- 「エラーが出たから消えていないだろう」と押し直すと、**意図した以上に研究データが失われる**

### 最小の修正

`js/store.js` の `BATCH_SIZE` の隣に宣言を追加する(値は **15000**(15 秒)で合意済み)。

```js
/**
 * 削除の確定を待つ上限。
 * 通信が切れると解決も棄却もしないことがあるため、必ず打ち切る。
 */
const COMMIT_TIMEOUT_MS = 15000;
```

### なぜ CI を通ってしまったか

`node --check` は未宣言変数を検出しない。整合性チェック 8 項目すべてに合格し、
GitHub Actions も通ったうえで公開された。**この穴を塞ぐ検査の追加が第 3 章 ④。**

---

## 3. 合意済みの実装方針(利用者の判断を得たもの)

利用者に確認し、次のとおり決定している。**この範囲は実装すること。**

| # | 内容 | 決定 |
|---|---|---|
| ① | `COMMIT_TIMEOUT_MS` の宣言 | **実施**。値は 15 秒 |
| ② | 打ち切り時の件数・文言の正確化 | **実施** |
| ③ | `describeError` が生の例外メッセージを露出する件 | **実施** |
| ④ | `tools/verify.js` に未宣言識別子の検査を追加 | **実施** |
| ⑤ | 一括削除の失敗表示の位置(`actionError`)を移す | **実施しない**(現状維持) |
| 版 | バージョン表記 | **v.02.1 へ繰り上げ**。`ProgressReport.md` / `REVIEW.md` に記録も追記 |

### 版を上げる際に必ず触るファイル

`tools/verify.js` の「バージョン表記の一致」が 5 箇所を横断で照合する。
1 つでも漏れると CI が落ちる。

1. `js/app.js` の `APP_VERSION`
2. `index.html` の `id="version"`
3. `RSD.md` の「現行バージョン」行
4. `ProgressReport.md` の「現行バージョン」行
5. `REVIEW.md` の「現行バージョン」行

### Firebase 側の操作は不要

`firestore.rules` を変更しなければ再デプロイは不要。②③④は JS のみ。

---

## 4. ②③④ の設計(前のセッションで到達した内容)

### ② 打ち切り時の件数を正確に伝える

**問題:** 打ち切り(タイムアウト)は「送ったが結果が分からない」状態。
サーバー側で後から確定しうるため、**消えたとも残ったとも言えない**。
現在のコードは `err.deleted` に「ここまでは確実に消えている」件数だけを添え、
残りを暗黙に「消えていない」扱いにしている。

**設計:** 削除の対象を 4 つに分けて、必ず全部を伝える。

| 区分 | 意味 |
|---|---|
| `deleted` | 確実に消えた件数 |
| `pending` | 送ったが結果を確認できなかった件数 |
| `untouched` | まだ送っていない件数(`total - deleted - pending`) |
| `skipped` | 進行中のため最初から対象外にした件数 |

**判定の方向:** 「消えなかった」と言い切れるのは**サーバーが拒んだ場合だけ**。
列挙するのは確実な側だけにし、**知らないコードは結果不明に寄せる**
(誤って「残っている」と伝えると、消えた記録を見落とす)。

```js
// js/store.js
const REJECTED = new Set([
  "permission-denied", "invalid-argument", "failed-precondition",
  "out-of-range", "unimplemented",
]);
// not-found は入れない。削除では「見つからない = すでに無い」であり、
// 残っていることの証明にはならない。
```

**その他の要点:**

- 送信の印(`sent`)は `batch.commit()` を**呼んだ後**に立てる。呼ぶ前に立てると、
  呼び出し自体が投げた場合(何も送っていない)まで結果不明になる
- まとめ送りの**組み立ても `try` の中**に入れる。外で投げると件数が付かず、
  すでに消えた記録を「残っている」と伝えてしまう
- 例外がオブジェクトでない場合は包んでから目印を付ける(代入自体が投げる)
- 進捗通知(`onProgress`)は `try` で囲む。通知が投げると件数を伝えられないまま失敗する
- 打ち切りは `deleteSessions` だけでなく、**全件読み出し**(`collectDeletable` /
  `fetchAllSessions`)と**1 件削除**(`deleteSession`)にも必要。
  打ち切らないと、書き出しと一括削除がどちらも始められなくなる
- 1 件削除の結果不明も、まとめ削除の `pending` と同じ扱いで伝える
  (`isUncertain(err)` を `store.js` から出して共通化)

### ③ 想定外の例外を利用者に出さない(ただし報告できる形は残す)

**問題:** `describeError` の末尾が `err.message` をそのまま返すため、
内部の識別子(`Can't find variable: COMMIT_TIMEOUT_MS`)が実験中の画面に出た。

**設計:** 利用者向けに書いた文言だけをそのまま出す。

```js
// js/store.js — そのまま画面に出してよい例外に目印を付ける
function userError(message) {
  const err = new Error(message);
  err.userMessage = message;
  return err;
}
```

```js
// js/app.js — describeError の末尾
if (table[code]) return table[code];
if (err?.userMessage) return err.userMessage;   // こちらが書いた文言だけ

// 実機は iPad でコンソールを開けない。詳細を消すと開発者に伝える手段が
// 無くなる(この不具合も、利用者が画面のメッセージを写して報告して分かった)。
console.error("[okulab-time] 想定外のエラー", err);
const raw = err?.message ?? (typeof err === "string" ? err : "");
const detail = [code, raw].filter(Boolean).join(": ").slice(0, 120);
return "予期しないエラーが発生しました。ページを再読み込みしてください。" +
  (detail ? `(詳細: ${detail})` : "");
```

**注意:** 利用者向けに文言を投げている箇所(`deriveRoomId` の暗号 API エラー、
削除の打ち切り)は `userError` に置き換える。置き換えないと文言が消える。

**もう一つの落とし穴:** 削除の件数を表示する欄に
「ページを再読み込みしてください」が併記されると、従うと件数が消える。
件数と同じ欄に出す文言からは、**文の切れ目にある**案内だけを外す。

```js
// 語句単位で消すと「通信状況を確認してもう一度お試しください。」が
// 「通信状況を確認して」で切れる。文の切れ目に限定する。
function reasonForReport(err) {
  return describeError(err)
    .replace(/(^|。)(?:ページを再読み込みしてください|もう一度お試しください)。/g, "$1");
}
```

### ④ 定数の宣言漏れ検査

`tools/verify.js` に 9 番目の検査として追加する。大文字の定数に限り、
参照先が同じファイルで宣言または import されているかを照合する。

**要件:**

- **誤検出をしないこと**(検査の失敗はデプロイを止める)。判断に迷う場合は
  「宣言されている」側に倒す。検出漏れのほうが害が小さい
- **検出すべき形を確実に捕まえること**:
  宣言の削除 / `const` の書き忘れ / 三項演算子の真の側 / `case` ラベル /
  初期値での参照 / 仮引数の既定値 / `for-of` の反復対象 / 分割代入の既定値 /
  class のメソッド内の代入 / 下線を含まない 1 語の定数(`REJECTED` など)
- **誤検出してはいけない形**(前のセッションで実際に踏んだもの):
  分割代入 / 複数宣言(`const A = 1, B = 2`)/ 複数行の分割代入 /
  初期値に関数を挟む複数宣言 / 別名つき `import` / `import D, { n } from` /
  副作用だけの `import` / class のインスタンス・静的・私有フィールド /
  1 行に詰めた class / メソッドと `constructor` の仮引数 / get アクセサ /
  ラベルと `break`/`continue` / ブロック直後の正規表現リテラル /
  正規表現内の引用符 / 後置インクリメント(`i++ / rate`)/
  括弧なし arrow(`MS => …`)/ 既定値に呼び出しを含む仮引数 /
  大文字の組み込み(`JSON` / `URL` / `CSS`)

**実装の骨格:**

1. `stripLiterals(src)` — 注釈・文字列・正規表現を除去。
   テンプレート文字列は本文だけ捨て、`${ }` の中は式なので残す。
   正規表現と割り算の判別は、直前の文字と語(`return` など)で行い、
   後置演算子(`++` / `--`)は割り算として扱う
2. `parenGroups(code)` — 対応の取れた丸括弧をすべて拾う(入れ子も)。
   仮引数に `f(1)` のような既定値を書けるため、単純な `[^()]*` では切れる
3. `statementBody(text)` — 宣言の終わりを**深さ 0 のセミコロン**で判定。
   初期値の関数内のセミコロンで切ると後ろの宣言を取りこぼす
4. `bindingTargets(text)` — 宣言本体から束縛される側だけを取り出す。
   初期値・既定値は**深さに関係なく**読み飛ばす(`{ limit = MAX_MS }` も)。
   セミコロン省略に備え、束縛を書き終えた位置の改行で打ち切る
5. `classBodyRanges(code)` — class 本体のうちフィールドを書ける位置だけ。
   **入れ子の波括弧は範囲から外す**(メソッド内の代入をフィールドと見なすと
   `const` の書き忘れを見逃す)
6. `isNamePosition(code, i, len)` — 値の参照ではなく名前として書かれているか。
   オブジェクトのキーとラベル(定義・`break` の参照)を除き、
   三項演算子の真の側と `case FOO:` は**参照として扱う**
7. `declaredNames(code)` — 束縛を広めに拾う。`const`/`let`/`var`、
   関数名と仮引数、arrow(括弧あり・なし)、class 名、`catch`、
   メソッドと `constructor`、class フィールド、`import`/`export` の指定子
8. 参照の抽出は `/(?<![.#\w$])([A-Z][A-Z0-9]*(?:_[A-Z0-9]+)*)\b/g`。
   下線は省略可、直前の `.`(属性)と `#`(私有)は除く
9. **抽出が 0 件なら不合格にする**(何も見ずに合格するのを防ぐ)
10. `JS_FILES` に `tools/verify.js` 自身も加える(この道具の欠陥は公開を止める)

あわせて、既存の「エラーコードと文言テーブル」検査が `js/store.js` しか
見ていないため、`js/app.js` も見るように広げる(`code: "CANCELLED"` が
`describeCode` に無いまま通っていた)。

---

## 5. レビューで見つかった欠陥と設計判断(51 巡ぶんの結論)

②③④の実装に伴って `js/app.js` の表示・状態管理を作り直した。
以下はそのとき見つかった欠陥と、採った方針。**同じ穴を踏まないための一覧。**

### 5.1 取り消せない操作の結果は、必ず伝わる形で残す

| 見つけた欠陥 | 採った方針 |
|---|---|
| 削除の件数が画面のエラー欄にしか残らず、計測操作・書き出し・退出・再読み込みで消えた | エラー欄を **2 段構え**(削除の内訳 + ほかの操作からの知らせ)にし、内訳は持ち主だけが消せるようにした |
| 退出後に終わった操作の結果が、どこにも出なかった | 退出後の結果は専用の欄(参加画面 / 実験者用画面の両方)に出し、`localStorage` に保存して再読み込みでも失わないようにした |
| その欄を接続の警告と共用したため、接続警告を消すつもりの操作で件数が消えた | 専用の欄に分けた |
| 欄そのものを押して消せるようにしたため、大きな計測ボタンの隣で誤って触れ、確認ダイアログが**次の押下を飲み込んだ** | 欄は押せないようにし、**専用の消去ボタン**を置いた(キーボードでも操作可) |
| 退出でエラー欄が消えるのに、退出の確認で何も伝えていなかった | 退出の確認に、消える控えをすべて載せる |
| 持ち越す知らせを上限で古い順に落としたため、ここにしか残らない削除の件数が真っ先に消えた | 取り返しのつく知らせ(成功の報告など)から落とし、結果不明と未保存は最後まで残す |
| 退出後の知らせを文言で重複判定したため、別のルームの結果が 1 件にまとまった | まとめず、**日時**(`formatFull`)と**ルーム**(`room.slice(0, 6)`)を添える |

**原則:** 取り消せない操作の結果は、画面から消えても追える形を必ず 1 つ残す
(コンソール + 保存)。**「消えたか分からない」は「消えなかった」と言い換えない。**

### 5.2 被験者の押下を絶対に落とさない

| 見つけた欠陥 | 採った方針 |
|---|---|
| 一括削除が `state.busy` を立てていたため、その間(数え上げと確定で 30 秒超)は計測の開始・終了が**無視されていた**。被験者用画面では失敗として数える経路も通らず、誰にも気づかれない | 一括削除は `state.busy` を立てない。専用の印(`clearHoldsBusy`)で持ち、**記録側の操作だけ**を止める。削除の対象は開始前に数え終えているため計測と重ならない |
| 退出後の結果を `alert` で出していたため、実験中に割り込んで押下を奪った(15 秒を超えると押下時刻が閉じた時刻になる) | `alert` をやめ、画面に残す形だけで伝える |
| 押しても何も起きないボタンがあった(判定と見た目の不一致) | ボタンの `disabled` は必ず判定と同じ条件から組み立てる |

**原則:** 「押した瞬間」を失う変更はしない。割り込むダイアログを実験中に出さない。

### 5.3 研究データを失わせない

| 見つけた欠陥 | 採った方針 |
|---|---|
| 共有シートを閉じて保存を取り消しても「書き出しました」と表示していた | `saveFile` は `"cancelled"` / `"unknown"` を返す。**共有の成功は保存の証明にならない**(クリップボードや断られた送信でも解決する)ため、取り消し以外は「確認できない」として扱う |
| ダウンロード経路は保存先の取り消しを知る手段がない | 同じく `"unknown"`。一括削除の確認で「保存されたか確認できません」と伝える |
| 一括削除の確認が、書き出した**件数**と現在の件数を比べていた。削除で減った分と増えた分が相殺され、未保存の記録を見落とす | 書き出しに含まれた**記録の id** を控え、消そうとしている記録のうち何件が含まれていないかで判断する |
| 書き出しの失敗で、それまでに書き出せていた範囲まで消していた | 失敗・取り消しではファイルが増えていないので、前の範囲はそのまま有効 |
| 書き出しの結果を 1 つしか持たず、別ルームで書き出すと前のルームの「未保存」が忘れられた | ルームごとに持つ |
| 書き出しの警告を 1 つに絞ったため、「保存できたか確認できない」が先に当たり「含まれていない記録がある」が隠れた | 当てはまるものはすべて並べて出す |

**原則:** 「保存できた」と言い切れない場合は言い切らない。
取り消せない操作の前には、**当てはまる警告をすべて**出す。

### 5.4 全件を読む操作どうしを重ねない(REVIEW.md 9-02 の再発防止)

| 見つけた欠陥 | 採った方針 |
|---|---|
| 入室時にボタン表示だけを戻していたため、前のルームの削除が走っている状態で書き出しを始められた | ボタンの状態は**走っている操作の数**(`recordsInFlight`)から組み立てる |
| 後始末に世代ガードをかけるとボタンが固まる(9-01)。かけないと古い操作が今の操作の状態を戻す | **所有権**(`recordsOwner` の通し番号)で判断する。世代ガードは使わない |
| 排他が片側だけだった(1 件削除が実行中の数を増やしていなかった) | 1 件削除も記録操作として数える |
| 共有シートの応答が返らないと、書き出しも削除もどのルームでも始められなくなった | サーバーとのやり取りが終わった時点で枠を返す。保存の待ちは別に数え、時間で打ち切る |
| 一括削除中も行ごとの削除ボタンが押せ、件数が食い違った | 記録操作が走っている間は押せないようにする |

### 5.5 非同期の後始末

- すべての非同期処理で、操作した時点の**ルームと世代を控えて照合**する
  (`state.roomId` を待ってから読み直すと、退出後に `null` を触って黙って失敗する)
- 退出後に終わった操作は、**結果を必ず伝える**(成功・失敗・結果不明のいずれも)。
  ただし**いまの画面の一覧を根拠にしない**(別のルームの件数が混ざる)
- 一括削除の最中も計測は続くため、開始時点の対象しか消えない。
  数が合わないときは「削除中に記録された分は残っています」と伝える

---

## 6. 既知の設計上の制約(仕様として残すもの)

前のセッションで利用者と確認した内容。**不具合ではない。**

### 6.1 記録終了の有効性は「時刻」では判定していない

被験者用UIの終了ボタンは常時押せる(計測状態を漏らさないため)。
有効かどうかの判定は `js/store.js` の `endSession` トランザクション内で、
**`meta/current.activeSessionId` と `status === "running"` だけ**を見ている。

判定していないこと:

- `endMs < startMs`(負の所要時間)は**拒否されない**。表示時に赤字で注意を出すだけ
- 経過時間の上限もない。開始から何時間後の押下でも有効な記録になる
- 時刻同期の精度は判定に使わない(`startSynced` / `endSynced` / `*AccuracyMs` に印を残すのみ)

### 6.2 端末間の時計ズレは補正済み。ただし残差がある

`js/clock.js` が `serverTimestamp()` を基準に SNTP 方式でオフセットを推定し、
押下時刻は補正済みの値(`Date.now() + offsetMs`)で記録する。
開始側・終了側の両方が同じ基準に写るため、**端末間のズレは相殺される**。

残る誤差:

- 各押下に `accuracyMs = RTT/2` の不確かさ。所要時間の誤差は最悪で両者の和
- 採用は 5 回試行の最小 RTT。`MAX_ACCURACY_MS = 1000` を超える標本は捨てるため、
  **最悪ケースでは片側 ±1 秒が通り得る**
- 往復の非対称によるバイアスは原理的に補正できない
- 一度も同期できていない / 同期が古い(6 分超)場合は `synced: false` を記録
- 再同期の間(最大 4 分)に OS の時刻補正が走ると、その分が記録値に乗る

被験者用UIでは未同期でも確認ダイアログを出さずに記録する(状態を漏らさないため)。

### 6.3 その他

- オフラインでは起動できない(Service Worker を持たない)
- 一覧表示は最新 300 件。CSV は全件を出力する
- 合言葉を知る利用者による偽の記録作成は防げない

---

## 7. 作業手順

### 環境

ビルド不要。`node` は整合性チェックにのみ使う(Node.js 22 で確認)。

```sh
git clone https://github.com/choucrin/Okulab_time.git
cd Okulab_time
git checkout claude/recording-end-validation-c7zblx
node tools/verify.js        # 8 件すべて合格することを確認
```

### 変更のたびに

```sh
node tools/verify.js        # 必ず通す。落ちたらデプロイされない
git add -A
git commit -F <メッセージファイル>
git push -u origin claude/recording-end-validation-c7zblx   # ← 毎回
```

### `js/store.js` の動作確認

Firestore に接続せずに `deleteSessions` の件数計算を確かめられる。
前のセッションでは次の方法で 13 件のテストを回した。

1. `js/store.js` を写し、`https://www.gstatic.com/firebasejs/...` の import を
   ローカルのスタブに差し替える
2. スタブは `writeBatch` を差し替え可能にしておく(`__setWriteBatch`)
3. 打ち切り経路を試す写しは `COMMIT_TIMEOUT_MS` を 200 に書き換える

確認した項目:

- 正常系(900 件 = 3 回のまとめ送り、進捗も 3 回)
- 拒否 → `deleted` のみ、`pending` なし
- 打ち切り → `deleted=0` / `pending=400` / `userMessage` あり
- 打ち切り後に届いた失敗が未処理拒否にならない
- 結果不明のコード 6 種 → `pending` あり
- 拒否のコード 3 種 → `pending` なし
- 未知のコード・コード無し → `pending` あり(既定で結果不明)
- 進捗の通知が投げても件数が伝わる
- 組み立ての失敗 → 送信前なので `pending` なし
- 非オブジェクトの例外 → 件数を保って伝わる
- `commit()` の同期例外 → `pending` なし(何も送っていない)
- `not-found` → `pending` あり
- `isUncertain` の判定

### `tools/verify.js` の検査を追加したら

**混入時の状態で不合格になることを必ず確認する。**
合格するだけの検査は、検査していないのと同じ。

```sh
# 例: 宣言を消して不合格になるか
sed -i 's/^const COMMIT_TIMEOUT_MS = 15000;/\/\/ removed/' js/store.js
node tools/verify.js        # NG になること
git checkout js/store.js
```

### コミットメッセージ

このリポジトリは日本語で、1 行目に要約、空行、本文に「何が問題で、なぜ直すのか」を書く。
`REVIEW.md` の番号(`9-02` など)を参照して経緯をつなぐ慣習がある。

末尾に付ける行:

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```

---

## 8. 残っている作業

1. **第 2 章の最小修正**(`COMMIT_TIMEOUT_MS` の宣言)。最優先。これだけで症状は消える
2. **第 3 章 ②③④** と版の繰り上げ(v.02.1)、`ProgressReport.md` / `REVIEW.md` への追記
3. **第 5 章の設計**を `js/app.js` に反映(表示・状態管理の作り直し)
4. レビュー(前のセッションでは Opus によるレビューを繰り返し、
   重大・重要な指摘が出なくなるまで対応した。51 巡で収束していない)

### `REVIEW.md` に追記すべき内容(前のセッションで書いた内容の要点)

- **致命的 10-01:** 一括削除が必ず失敗し、記録は実際に消えているのに
  「失敗」として報告される(第 2 章)
- **重要 10-02:** 打ち切った送信を「消えなかった」と扱っていた
- **重要 10-03:** 想定外の例外の生メッセージをそのまま利用者に表示していた
- **重要 10-04:** 構文チェックは未宣言変数を検出せず、整合性チェック 8 件すべてに
  合格したまま公開されていた

**教訓として残す価値があるもの:**

1. **「成功しているのに失敗と表示される」は、失敗するより危険。**
   利用者は表示を信じて操作をやり直す。取り消せない操作では、その繰り返しが
   そのままデータの損失になる
2. **不具合の修正そのものが、次の不具合を持ち込む。**
   10-01 は 9-03(通信断で固まる問題の修正)が原因
3. **機械的な検査は、通ったことを根拠にしてはならない。**
   不具合を見つけたら、**同じ不具合を検出できる検査を足してから修正を終える**
4. **ローカルのコミットは成果ではない。push するまで存在しない**(このセッションの教訓)
