# 開発ログ — Okulab Time

- 現行バージョン: **v.02.1**
- リポジトリ: https://github.com/choucrin/Okulab_time
- 関連文書: [RSD.md](RSD.md)(要件定義)/ [REVIEW.md](REVIEW.md)(レビュー記録)/ [README.md](README.md)(構築・利用手順)

---

## 1. 環境選定の経緯

利用者から「普段の GitHub Pages + Firebase 構成で、2 端末計測アプリを作れるか」という
問い合わせがあり、次のとおり回答した。

**結論: 現行構成のまま実現可能。** Firestore のリアルタイム同期と相性が良い。

判断の要点:

| 論点 | 判断 |
|---|---|
| バックエンドサーバーの要否 | 不要。ブラウザから Firebase SDK を直接呼ぶ |
| Cloud Functions の要否 | **不要**。経過時間の計算はクライアント側で完結できる。Cloud Functions を使うと Blaze プラン(従量課金)への切り替えが必須になるため、利用者の判断で不使用とした |
| 時刻の取得方法 | `serverTimestamp()` は書き込みタイミングの影響を受けるため、ボタン押下時の `Date.now()` を使用。ただし端末間の時計ズレが誤差になるため、サーバー時刻を基準としたオフセット補正を実装する方針とした |
| コスト | Firestore 無料枠(読み取り 5 万 / 書き込み 2 万・日)で十分。追加コストなし |

代替案として Supabase、Cloudflare Pages + D1/KV も提示したが、現行構成を変更する理由は
特にないと判断した。

---

## 2. 設計上の確認事項と決定

開発開始前に 4 点を確認し、以下のとおり決定した。

| # | 論点 | 決定 |
|---|---|---|
| 1 | 端末の役割 | **画面上で切り替え可能**とする(開始 / 終了 / 閲覧)。どちらの端末をどちらに割り当ててもよい |
| 2 | 付帯情報 | **任意ラベル 1 つ**。空欄でも計測可能 |
| 3 | アクセス制限 | **匿名認証 + 合言葉**。合言葉の SHA-256 をルーム ID とし、合言葉自体は送信・保存しない |
| 4 | 開始と終了の対応付け | **1 本ずつ順に**。同時並行は許可しない。トランザクションで排他制御 |

---

## 3. 主要な技術判断

### 3-1. 時刻精度の確保

本アプリの中核。2 台が別々に `Date.now()` を読むと、**端末どうしの時計のズレが
そのまま計測誤差**になる(iOS の自動時刻設定でも数十〜数百 ms ずれる)。

対策として次を実装した。

- Firestore の `serverTimestamp()` を共通の基準時計とし、SNTP と同じ方式で
  「サーバー時刻 − 自端末時刻」のオフセットを推定
- 書き込み直前・直後のローカル時刻でサーバーのタイムスタンプを挟み込み、
  往復が最短だった試行を採用
- 区間測定には `performance.now()` を使用(OS の時刻補正で標本が壊れないため)
- 参加時 5 回、以降 4 分ごと、画面復帰・通信回復時に再測定
- 押下時刻は `click` ではなく **`pointerdown`(指が触れた瞬間)** で取得
- 補正前の生値・補正量・推定誤差も併せて保存し、事後検証を可能にする

### 3-2. Cloud Functions を使わない排他制御

開始・終了の対応付けを `meta/current` ドキュメントと Firestore トランザクションで実現。
サーバーサイドのコードなしに「同時に 2 本走る」事故を防いでいる。

### 3-3. 被験者用画面の設計

被験者は実験の内容を知らされていない。したがってこの画面では、
**計測が動いているかどうかを推測させる手がかりを一切置かない**。

- 状態表示・経過時間・記録一覧・トーストをすべて除外した
- ボタンは常に押せ、送信も条件を付けずに行う。可否はサーバーが判断する
- **押したときの反応を、計測中かどうかで変えない。** 成否にかかわらず必ず文章を切り替える。
  変化の有無そのものが「いま計測が動いている」という手がかりになるため
- 未補正の時刻に対する確認ダイアログも、この画面では出さない
- 読み物は、画面に自然と視線が向かうようにするためのもの。
  時間の経過を意識させる語(計測・合図・時計など)を避けて書き下ろした
- 端末や文章の長さがまちまちでもスクロールが出ないよう、
  枠に収まるまで文字サイズを自動で下げる

### 3-4. 通信断への耐性

研究用計測では「押した瞬間」が失われることが最も痛い。そのため、

- 送信失敗時は押下時刻を保持したまま自動再送(最大 20 秒)
- 再送が二重登録にならないよう、セッション ID を事前採番して冪等化
- 再送中に状況が変わった場合、別のセッションを巻き添えにしない照合を実装

---

## 4. 作業経過

### 2026-08-01

| 時点 | バージョン | 内容 |
|---|---|---|
| 初版実装 | — | 全ファイルを作成し、GitHub にプッシュ |
| 1 巡目レビュー | — | Opus・Sonnet を並列実行。致命的 2 件・重要 6 件・軽微 16 件を検出 |
| 1 巡目修正 | — | 上記を反映してプッシュ |
| 2 巡目レビュー | — | 致命的 1 件・重要 5 件を検出(うち一部は 1 巡目の修正で新規混入) |
| 2 巡目修正 | — | 上記を反映してプッシュ |
| 3 巡目レビュー | — | 致命的 3 件・重要 3 件を検出。**すべて 2 巡目の修正で新規混入** |
| 3 巡目修正 | — | 上記を反映してプッシュ |
| 4 巡目レビュー | — | 致命的 0 件・重要 2 件。競合処理 8 ケースの検証を通過し収束 |
| 4 巡目修正 | v.01.0 | 上記を反映。要件定義書・レビュー記録・開発ログを整備 |
| 5 巡目レビュー | — | 致命的 0 件・重要 2 件(うち 1 件は 4 巡目の修正で新規混入) |
| 5 巡目修正 | v.01.1 | 上記を反映 |
| 6 巡目レビュー | — | **致命的 0 件・重要 0 件。両モデルとも指摘なしで収束** |
| 6 巡目修正 | v.01.2 | 軽微指摘(表示位置・文書の巡数表記)のみ反映 |

検出した不具合の詳細は [REVIEW.md](REVIEW.md) に集約している。

### 2026-08-10

| 時点 | 内容 |
|---|---|
| 環境構築 | 利用者が Firebase コンソール側の設定を完了。`firebaseConfig` の値を反映 |
| セキュリティ確認 | 公開 API キーに対する GitHub の検出について、履歴の秘密情報スキャンと実地のアクセス検証を実施(下記) |
| 公開方式の変更 | Deploy from a branch → **GitHub Actions**。整合性チェックを CI 化し、失敗時はデプロイしない |
| 実機不具合の修正 | 実機運用で購読が無言で停止する不具合が判明(3 症状)。生存監視と自動復旧を追加(v.01.3) |
| 修正のレビュー | 二重レビューで、生存監視の追加が致命的 2 件を含む 8 件の新たな欠陥を生んでいたことが判明。対応(v.01.4) |
| 機能追加 | 被験者用画面を追加。Windows 向けの日本語フォントを補強(v.01.5) |
| 機能追加のレビュー | 二重レビューで致命的 1 件を含む 10 件を検出。押下への反応の差が状態を漏らしていた。対応(v.01.6) |
| 機能追加 | 記録の一括削除を追加(v.01.7) |
| 実機の指摘 | 被験者用画面の文量が不足していたため約 1.5 倍に増量し、枠内スクロールを許容(v.01.8) |
| 追加分のレビュー | 二重レビューで致命的 1 件を含む 7 件を検出。離脱時にボタンが固まり、書き出しが壊れる経路があった。対応(v.01.9) |
| 実機の指摘 | 読み物をさらに増量(平均 270 → 383 字)(v.02.0) |

### 2026-09-17

| 時点 | 内容 |
|---|---|
| 実機の不具合 | 一括削除の直後に `Can't find variable: COMMIT_TIMEOUT_MS` が表示される、と実機から報告。v.01.9 で打ち切りを追加した際、**定数の宣言だけが漏れていた**。削除自体は送信済みのため成功しているのに失敗として報告される、最も気づきにくい形の不具合だった。対応(v.02.1) |
| 再発防止 | 構文チェックが未宣言変数を検出できないことが原因のため、整合性チェックに定数の宣言検査を追加(検査ツール自身も対象)。あわせて、想定外の例外の文言をそのまま画面に出さないようにし、削除の内訳を漏れなく伝えるようにした(v.02.1) |

---

## 5. 品質確認の方法

以下を機械的に検証している。[`tools/verify.js`](tools/verify.js) に実装し、
`main` への push 時に GitHub Actions で自動実行する。
**検証に失敗した場合はデプロイされない。**手元では `node tools/verify.js` で確認できる。

| 項目 | 内容 |
|---|---|
| 構文 | 全 JS ファイルを ES モジュールとして構文チェック |
| HTML ↔ JS | `id` 属性と `getElementById` の対応が完全一致するか |
| HTML/JS ↔ CSS | 使用しているクラスがすべて CSS に定義されているか |
| **Firestore ルール ↔ 送信データ** | `create` 18 フィールド・`update` 9 項目/3 項目・`meta` 2 キーを 1 つずつ照合。**1 つでもずれると計測開始が拒否され実験が止まるため、毎回実施** |
| エラーコード | 各処理が返すコードがすべて文言テーブルに存在するか(`store.js` と `app.js` の両方) |
| バージョン | 全ファイルのバージョン表記が一致するか |
| 設定 | Firebase 設定がプレースホルダのままでないか |
| **定数の宣言** | 大文字定数の参照先が同じファイルで宣言・import されているか。**構文チェックでは未宣言変数を検出できず、v.02.0 で実際に公開まで通してしまったため追加**(v.02.1) |

### セキュリティの実地確認(2026-08-10)

`firebaseConfig` の公開に対し GitHub の Secret Scanning が反応したため、次を検証した。

- **全コミット履歴のスキャン**: 秘密鍵・サービスアカウント・各種アクセストークンの検出はゼロ
- **未認証での読み取り**: 拒否(403)
- **ルーム ID の列挙**: 拒否(403)。合言葉を知らない限りデータに到達できないことを確認
- **コレクション名の列挙**: 拒否(403)
- **他人の識別子を騙った書き込み**: 拒否(403)
- **想定外フィールドの混入**: 拒否(403)
- **正規のルーム ID でのアクセス**: 許可(200)。ルールが正常な操作を妨げていない

Web API キーは設計上公開される識別子であり、秘匿は不可能かつ不要。
アクセス制御はセキュリティルールが担っており、上記のとおり実効性を確認した。
なお、キーの悪用による無料枠の消費を抑えるため、
Google Cloud コンソールで **HTTP リファラ制限**をかけることを推奨する。

---

## 6. 既知の制約

| # | 制約 | 理由 |
|---|---|---|
| L-1 | オフラインでは起動できない | Service Worker を持たないため。実験中は接続の確認が必要 |
| L-2 | 数ミリ秒以下の精度は保証できない | ネットワーク経由の時刻同期には原理的な限界がある |
| L-3 | キーボード操作時は精度がわずかに劣化 | `pointerdown` が発生せず `click` の時刻になるため |
| L-4 | 一覧表示は最新 300 件 | 読み取り回数の抑制のため。CSV は全件を出力する |
| L-5 | 合言葉を知る利用者による偽の記録作成は防げない | 正規の操作と区別がつかないため。合言葉は推測されにくい文字列にすること |

---

## 7. 今後の課題(未着手)

- 実機での時刻精度の実測(動作確認は 2026-08-10 に実施し、購読停止の不具合を検出・修正済み)
- 長時間・多数回の計測における無料枠消費の実測
- 必要に応じた Service Worker の導入(オフライン起動)

---

## 変更履歴

| 日付 | バージョン | 内容 |
|---|---|---|
| 2026-08-01 | v.01.0 | 初版リリース。4 巡のレビューを経て収束。本ログを新規作成 |
| 2026-08-01 | v.01.1 | 5 巡目の指摘(終了時の文言判定・バージョン表記の表示位置)を反映 |
| 2026-08-01 | v.01.2 | 6 巡目で収束を確認。軽微な表示調整のみ |
| 2026-08-10 | v.01.2 | Firebase 設定を反映。公開方式を GitHub Actions に変更し、整合性チェックを CI 化 |
| 2026-08-10 | v.01.3 | 購読の無言停止に対する生存監視と自動復旧を追加。実機で報告された 3 症状に対応 |
| 2026-08-10 | v.01.4 | 生存監視が持ち込んだ欠陥を修正。接続遮断を廃止し、トランザクション結果の即時反映と操作可能性の確保に方針転換 |
| 2026-08-10 | v.01.5 | 被験者用画面(読み物つき・状態非表示)を追加。Windows 対応を明記 |
| 2026-08-10 | v.01.6 | 被験者用画面の情報漏れ・押下の取りこぼし・レイアウト調整の不具合を修正 |
| 2026-08-10 | v.01.7 | 記録の一括削除を追加(二段階確認・進行中は保持・全件をサーバーから取得) |
| 2026-08-10 | v.01.8 | 読み物を平均 186 字から 270 字へ増量。収まらない場合は枠内でスクロールできるようにした |
| 2026-08-10 | v.01.9 | 一括削除が書き出しを壊す経路と、離脱時にボタンが固まる不具合を修正 |
| 2026-08-10 | v.02.0 | 読み物を平均 270 字から 383 字へ増量(最長 490 字) |
| 2026-09-17 | v.02.1 | 一括削除が未宣言の定数で失敗する不具合を修正。定数の宣言検査を整合性チェックに追加し、想定外の例外の露出と削除の内訳の伝え方を見直した |
