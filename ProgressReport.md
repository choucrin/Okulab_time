# 開発ログ — Okulab Time

- 現行バージョン: **v.01.2**
- リポジトリ: https://github.com/choucrin/Okulab_time
- 関連文書: [RSD.md](RSD.md)(要件定義)/ [REVIEW.md](REVIEW.md)(レビュー記録)/ [README.md](README.md)(構築・利用手順)

---

## 1. 環境選定の経緯

利用者から「普段の GitHub Pages + Firebase 構成で、2 端末計測アプリを作れるか」という
問い合わせがあり、次のとおり回答した。

**結論: 現行構成のまま実現可能。** Firestore のリアルタイム同期と相性が良い。

判断の要点:

| 論点 | 判断 |
|---|---|
| バックエンドサーバーの要否 | 不要。ブラウザから Firebase SDK を直接呼ぶ |
| Cloud Functions の要否 | **不要**。経過時間の計算はクライアント側で完結できる。Cloud Functions を使うと Blaze プラン(従量課金)への切り替えが必須になるため、利用者の判断で不使用とした |
| 時刻の取得方法 | `serverTimestamp()` は書き込みタイミングの影響を受けるため、ボタン押下時の `Date.now()` を使用。ただし端末間の時計ズレが誤差になるため、サーバー時刻を基準としたオフセット補正を実装する方針とした |
| コスト | Firestore 無料枠(読み取り 5 万 / 書き込み 2 万・日)で十分。追加コストなし |

代替案として Supabase、Cloudflare Pages + D1/KV も提示したが、現行構成を変更する理由は
特にないと判断した。

---

## 2. 設計上の確認事項と決定

開発開始前に 4 点を確認し、以下のとおり決定した。

| # | 論点 | 決定 |
|---|---|---|
| 1 | 端末の役割 | **画面上で切り替え可能**とする(開始 / 終了 / 閲覧)。どちらの端末をどちらに割り当ててもよい |
| 2 | 付帯情報 | **任意ラベル 1 つ**。空欄でも計測可能 |
| 3 | アクセス制限 | **匿名認証 + 合言葉**。合言葉の SHA-256 をルーム ID とし、合言葉自体は送信・保存しない |
| 4 | 開始と終了の対応付け | **1 本ずつ順に**。同時並行は許可しない。トランザクションで排他制御 |

---

## 3. 主要な技術判断

### 3-1. 時刻精度の確保

本アプリの中核。2 台が別々に `Date.now()` を読むと、**端末どうしの時計のズレが
そのまま計測誤差**になる(iOS の自動時刻設定でも数十〜数百 ms ずれる)。

対策として次を実装した。

- Firestore の `serverTimestamp()` を共通の基準時計とし、SNTP と同じ方式で
  「サーバー時刻 − 自端末時刻」のオフセットを推定
- 書き込み直前・直後のローカル時刻でサーバーのタイムスタンプを挟み込み、
  往復が最短だった試行を採用
- 区間測定には `performance.now()` を使用(OS の時刻補正で標本が壊れないため)
- 参加時 5 回、以降 4 分ごと、画面復帰・通信回復時に再測定
- 押下時刻は `click` ではなく **`pointerdown`(指が触れた瞬間)** で取得
- 補正前の生値・補正量・推定誤差も併せて保存し、事後検証を可能にする

### 3-2. Cloud Functions を使わない排他制御

開始・終了の対応付けを `meta/current` ドキュメントと Firestore トランザクションで実現。
サーバーサイドのコードなしに「同時に 2 本走る」事故を防いでいる。

### 3-3. 通信断への耐性

研究用計測では「押した瞬間」が失われることが最も痛い。そのため、

- 送信失敗時は押下時刻を保持したまま自動再送(最大 20 秒)
- 再送が二重登録にならないよう、セッション ID を事前採番して冪等化
- 再送中に状況が変わった場合、別のセッションを巻き添えにしない照合を実装

---

## 4. 作業経過

### 2026-08-01

| 時点 | バージョン | 内容 |
|---|---|---|
| 初版実装 | — | 全ファイルを作成し、GitHub にプッシュ |
| 1 巡目レビュー | — | Opus・Sonnet を並列実行。致命的 2 件・重要 6 件・軽微 16 件を検出 |
| 1 巡目修正 | — | 上記を反映してプッシュ |
| 2 巡目レビュー | — | 致命的 1 件・重要 5 件を検出(うち一部は 1 巡目の修正で新規混入) |
| 2 巡目修正 | — | 上記を反映してプッシュ |
| 3 巡目レビュー | — | 致命的 3 件・重要 3 件を検出。**すべて 2 巡目の修正で新規混入** |
| 3 巡目修正 | — | 上記を反映してプッシュ |
| 4 巡目レビュー | — | 致命的 0 件・重要 2 件。競合処理 8 ケースの検証を通過し収束 |
| 4 巡目修正 | v.01.0 | 上記を反映。要件定義書・レビュー記録・開発ログを整備 |
| 5 巡目レビュー | — | 致命的 0 件・重要 2 件(うち 1 件は 4 巡目の修正で新規混入) |
| 5 巡目修正 | v.01.1 | 上記を反映 |
| 6 巡目レビュー | — | **致命的 0 件・重要 0 件。両モデルとも指摘なしで収束** |
| 6 巡目修正 | v.01.2 | 軽微指摘(表示位置・文書の巡数表記)のみ反映 |

検出した不具合の詳細は [REVIEW.md](REVIEW.md) に集約している。

---

## 5. 品質確認の方法

各巡の修正後、以下を機械的に検証している。

| 項目 | 内容 |
|---|---|
| 構文 | 全 JS ファイルを ES モジュールとして構文チェック |
| HTML ↔ JS | `id` 属性と `getElementById` の対応が完全一致するか |
| HTML/JS ↔ CSS | 使用しているクラスがすべて CSS に定義されているか |
| **Firestore ルール ↔ 送信データ** | `create` 18 フィールド・`update` 9 項目/3 項目・`meta` 2 キーを 1 つずつ照合。**1 つでもずれると計測開始が拒否され実験が止まるため、毎回実施** |
| エラーコード | 各処理が返すコードがすべて文言テーブルに存在するか |

---

## 6. 既知の制約

| # | 制約 | 理由 |
|---|---|---|
| L-1 | オフラインでは起動できない | Service Worker を持たないため。実験中は接続の確認が必要 |
| L-2 | 数ミリ秒以下の精度は保証できない | ネットワーク経由の時刻同期には原理的な限界がある |
| L-3 | キーボード操作時は精度がわずかに劣化 | `pointerdown` が発生せず `click` の時刻になるため |
| L-4 | 一覧表示は最新 300 件 | 読み取り回数の抑制のため。CSV は全件を出力する |
| L-5 | 合言葉を知る利用者による偽の記録作成は防げない | 正規の操作と区別がつかないため。合言葉は推測されにくい文字列にすること |

---

## 7. 今後の課題(未着手)

- 実機(iPad + iPhone)での動作確認および時刻精度の実測
- 長時間・多数回の計測における無料枠消費の実測
- 必要に応じた Service Worker の導入(オフライン起動)

---

## 変更履歴

| 日付 | バージョン | 内容 |
|---|---|---|
| 2026-08-01 | v.01.0 | 初版リリース。4 巡のレビューを経て収束。本ログを新規作成 |
| 2026-08-01 | v.01.1 | 5 巡目の指摘(終了時の文言判定・バージョン表記の表示位置)を反映 |
| 2026-08-01 | v.01.2 | 6 巡目で収束を確認。軽微な表示調整のみ |
