# レビュー記録 — Okulab Time

Opus・Sonnet の二重レビューで検出した不具合と機能改善の記録。
新規開発・改修の前に、同種の問題を再発させていないか本ファイルを参照すること。

- 現行バージョン: **v.01.1**
- レビュー実施: 4 巡(各巡で Opus・Sonnet を並列実行)

---

## 総括

| 巡 | 致命的 | 重要 | 軽微 | 備考 |
|---|---|---|---|---|
| 1 巡目 | 2 | 6 | 16 | 初版に対するレビュー |
| 2 巡目 | 1 | 5 | 4 | 1 巡目の修正で新規混入したものを含む |
| 3 巡目 | 3 | 3 | — | **すべて 2 巡目の修正で新規混入** |
| 4 巡目 | 0 | 2 | — | 収束 |
| 5 巡目 | 0 | 2 | — | うち 1 件は 4 巡目の修正で新規混入 |

### 得られた教訓

1. **修正が新たな致命的欠陥を生む。** 3 巡目の致命的 3 件はすべて 2 巡目に自分で入れた修正が原因だった。
   競合制御・再送処理のような並行性に関わる修正は、必ず修正後にもレビューを回すこと。
2. **デスクトップ検証では発見できない不具合がある。**(#1-01)
   タッチイベントの発火順序に依存する問題は、マウス操作では正常に動作してしまう。
3. **「静かに劣化する」不具合が最も危険。** 精度が落ちる・データが上限で切られる・
   成功しているのにエラーが出る、といった症状は、実験を終えてから気づくことになる。
4. **セキュリティルールと送信データの突き合わせは機械的に検証する。**
   1 フィールドでもずれると計測開始が拒否され、実験が止まる。毎回スクリプトで照合している。

---

## 1 巡目(初版レビュー)

### 致命的

| # | 内容 | 対応 |
|---|---|---|
| 1-01 | **`pointerleave` が押下時刻を常に破棄していた。** タッチ入力ではポインタが `pointerup` 直後に消滅し、仕様上かならず `pointerleave` が発火する。そのため `click` 時点で `pressedAt` が必ず null になり、「指が触れた瞬間を記録する」機能がタッチでは完全に無効だった。マウス操作では正常に動くため PC 検証では発見不能。指を離すまでの時間(数十〜200ms、被験者ごとにばらつく)がそのまま誤差に乗っていた | 押下の取り消しを `pointerType === "mouse"` に限定 |
| 1-02 | **記録が最新 300 件で無言に打ち切られていた。** CSV 書き出しが購読結果をそのまま使っていたため、301 件目以降は画面からも CSV からも取り出せなくなる。件数表示も 300 で頭打ち | CSV を `getDocs` による全件取得に変更。一覧は 300 件のままとし、上限到達時に注記を表示 |

### 重要

| # | 内容 | 対応 |
|---|---|---|
| 1-03 | 時計同期の完了前に計測でき、補正なしの記録が判別不能なまま混入する | `startSynced`/`endSynced` を記録。未同期時は確認ダイアログを表示 |
| 1-04 | 押下時に確定すべき補正量・推定誤差を、送信時に読み直していた。再同期が挟まると生値との対応が壊れる | `ClockSync.snapshot()` で押下時点の状態を切り出す方式に変更 |
| 1-05 | 送信失敗で押下時刻が失われ、押し直しを強いられる | 押下時刻を保持したまま指数バックオフで自動再送 |
| 1-06 | `meta/current` と `sessions` の乖離でデッドロックしうる(開始も終了も中止もできない) | `meta/current` を購読し、計測中判定の正とする |
| 1-07 | 購読が切れた後、再購読せず同期が死んだまま動作を続ける | 指数バックオフによる自動再接続 |
| 1-08 | セキュリティルールの検証が不十分(`meta` 無検証、`endedBy` 詐称可能、更新フィールド無制限) | `hasOnly()` / `affectedKeys()` による形状固定、操作者 ID の照合を追加 |

### 軽微(主なもの)

- 起動〜認証完了まで画面が真っ白 → 接続中画面を追加
- `clock.stop()` が実行中の同期を中断しない → 中断フラグを追加
- 区間測定に `Date.now()` を使用(OS の時刻補正で標本が壊れる)→ `performance.now()` に変更
- `visibilitychange` だけでは iOS の復帰を取りこぼす → `pageshow` / `focus` / `online` を追加
- 退出後も前ルームの記録が表示され続ける → 退出時に再描画
- `auth/unauthorized-domain` のエラー文言が未整備 → 追加
- `apple-touch-icon` に SVG を指定(iOS は PNG のみ対応)→ PNG を生成
- standalone 表示で CSV ダウンロードが失敗しうる → 共有シート経由にフォールバック
- `color-mix()` / `100dvh` にフォールバックなし → 追加
- 削除ボタンのタップ領域が 44pt 未満 → 拡大

---

## 2 巡目(1 巡目の修正に対するレビュー)

### 致命的

| # | 内容 | 対応 |
|---|---|---|
| 2-01 | **送信リトライ中に「計測を中止」が押せた。** 終了処理が再送中に中止すると、確定寸前の終了時刻が捨てられ `aborted` で確定する。リトライ機能(1-05)を入れたことで新たに生まれた穴 | 送信中は中止ボタンを無効化 |

### 重要

| # | 内容 | 対応 |
|---|---|---|
| 2-02 | 中止ボタンの強制表示が直後の `renderControls()` で打ち消され、**1 巡目の復旧経路の修正が無効化されていた** | `state.abortHint` を介して描画に反映 |
| 2-03 | 再接続が全エラーを「接続断」に丸めて無限リトライ。Firestore 未作成や無料枠超過でも原因が表示されない | 再試行対象を限定し、それ以外は原因を表示して停止。回数上限も追加 |
| 2-04 | 再送が「成功しているのにエラー表示」になる。コミット済みで応答だけ失われた場合、再送すると `ALREADY_RUNNING` が返る | セッション ID を事前採番し、自分の書き込みを検出して成功扱いにする(冪等化) |
| 2-05 | 精度 ±5 秒の標本でも `synced: true` として記録され、警告が出ない。同期が 6 分以上古くても同様 | 採用上限を ±1 秒に引き締め、古い同期は未補正扱い。誤差の大きい記録を一覧で警告表示 |
| 2-06 | 送信中は退出も含め全操作が最大数十秒ロックされる | 送信に 20 秒の打ち切りを設定。送信中でも退出可能に |

### 軽微(主なもの)

- 長押し 2 秒超で押下時刻が click 時刻に差し替わる → 猶予を 15 秒に
- マルチタッチの 2 本目で押下時刻が上書きされる → `isPrimary` を判定
- 押下時刻の鮮度判定だけ `Date.now()` のまま → `performance.now()` に統一
- `at` と `rawAt` で `Date.now()` を 2 回呼び、最大 1ms ずれる → 壁時計の読みを 1 回に
- ライトモードの警告色が WCAG AA 未達 → 配色を調整

---

## 3 巡目(2 巡目の修正に対するレビュー)

> **この巡で検出した致命的 3 件は、すべて 2 巡目の修正で新規に混入したものだった。**

### 致命的

| # | 内容 | 対応 |
|---|---|---|
| 3-01 | **再送処理が別のセッションを誤って終了させる。** 再送の照合が「進行中フラグが空のとき」にしか置かれておらず、フラグが別のセッションを指す場合を見ていなかった。応答喪失で再送している間に相手端末が次の計測を開始すると、**始まったばかりの計測を前の試行の時刻で終了**させる。経過時間が負になり、押し直した時刻は復元不能。しかも成功トーストが出るため検知できない。Firestore がトランザクションを内部再実行するだけでも成立する(通信障害不要) | 食い違いを検出したら何も書かず `SESSION_CHANGED` を返す |
| 3-02 | 同上の欠陥が中止処理にも存在。次の計測を誤って中止する | 同上 |
| 3-03 | **退出後の再送が別のルームに書き込まれる。** 送信先 `roomId` をクロージャでライブ参照していたため、再送中に退出して別の合言葉で参加すると、古い操作が新ルームに書き込まれる | 送信先を操作時点で固定。ルーム世代カウンタで退出後の再送を打ち切る |

### 重要

| # | 内容 | 対応 |
|---|---|---|
| 3-04 | 開始の再送で、既に終了済みのドキュメントを `running` に上書きしようとしていた。ルールが弾いていたため実害はなかったが、誤った原因表示が出る(ルールだけが最後の砦になっていた) | 実体の存在確認を追加 |
| 3-05 | 購読エラーと操作エラーの表示先が同じで、片方の購読の復帰や次の操作で恒久エラーの表示が消える。**原因不明のまま操作不能**になる | 接続エラーを別枠に分離 |
| 3-06 | 同期失敗ピルの背景が半透明合成で、ライトモードのコントラストが 3.9:1 に低下 | 不透明な面に変更(5.4:1) |

---

## 4 巡目(3 巡目の修正に対するレビュー)

Opus が `store.js` の競合処理 8 ケース(正常系・再送・競合・自動再実行など)をすべて検証し、
**いずれも正しい**と判定。致命的な指摘はなし。

### 重要

| # | 内容 | 対応 |
|---|---|---|
| 4-01 | **`withBusy` の後始末が世代を見ていない。** 退出→別ルーム再参加後に、古い送信の完了処理が新しいルームの「送信中」ロックを解除し、二重送信の窓が開く。3 巡目で書き込み結果は守ったが、UI の状態フラグを守り忘れていた | `withBusy` に世代を渡し、一致時のみ状態を戻す |
| 4-02 | 購読のデータ到着側に停止ガードがない(エラー側とは非対称)。退出直前のスナップショットが新ルームの状態を汚染しうる | データ側にも `stopped` ガードを追加 |

### 併せて対応した指摘

- 2 台が同時に終了した場合の文言が実態と食い違う(「もう一方の端末で開始してください」)→ `ALREADY_ENDED` を新設
- 開始の再送で対象が既に終了済みの場合、「計測を開始しました」と表示されて紛らわしい → 状態に応じた文言に変更
- 送信済みの操作が退出後もサーバーに残りうる点が利用者に伝わらない → 退出確認の文言に明記
- `CANCELLED` が到達不能コードになっていた → 削除

---

---

## 5 巡目(4 巡目の修正に対するレビュー)

### 重要

| # | 内容 | 対応 |
|---|---|---|
| 5-01 | **`ALREADY_ENDED` の追加が `SESSION_CHANGED` の判定を巻き込んでいた。** 4 巡目で「2 台が同時に終了した場合」の文言として追加した分岐が `activeId` を見ておらず、**別の計測が進行中の場合まで「すでに終了しています」と表示**していた。書き込み保護(3-01)は無傷でデータ破壊はないが、利用者が再操作しないため終了の押下が丸ごと欠落する | `activeId` の有無で `SESSION_CHANGED` と `ALREADY_ENDED` を出し分け |
| 5-02 | バージョン表記を計測画面の内側に置いていたため、`[hidden]` によって**設定不備・参加前の画面では表示されない**。どのビルドかを確認したいのはむしろそれらの場面 | 画面要素の外に出し、固定表示に変更 |

### 補足(対応不要と判断)

- `send()` の打ち切りはリトライ「間隔」にしか効かないため、`operation()` が解決も棄却もしない場合(オフライン下の `runTransaction`)は `busy` が残りうる。HEAD 以前からの性質で、実際には SDK が必ず解決・棄却するため実害は確認されていない。
- 2 台の押下がミリ秒単位で完全一致すると、独立した 2 操作が再送と判定される。記録される値は同一のため実害なし。

---

## 変更履歴

| 日付 | バージョン | 内容 |
|---|---|---|
| 2026-08-01 | v.01.0 | 4 巡分のレビュー記録を集約して新規作成 |
| 2026-08-01 | v.01.1 | 5 巡目の記録を追記 |
